#pragma once

//#include <vector>

class Vertex
{

public:

  short x;
  short y;
  short z;

  Vertex()
  {
  }
    
  Vertex(short _x, short _y, short _z)
  {
    x = _x;
    y = _y;
    z = _z;
  }

  
};

//using Vertices = std::vector<Vertex>;
